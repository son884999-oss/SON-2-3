# DeepCheck 서비스 기획서

## 1. 서비스 개요

- 서비스명: DeepCheck
- 목적: 재무제표를 처음 접하는 사용자가 기업의 기초 체력과 위험을 짧은 시간 안에 파악하도록 돕는다.
- 타겟 사용자: 재무 용어가 낯선 주식 초보자, 여러 기업을 간단히 비교하려는 일반 투자자
- 해결하려는 문제: 금융 정보 서비스는 숫자와 전문 용어가 많아 지표의 의미와 판단 근거를 함께 이해하기 어렵다.
- 핵심 가치: 숫자, 비교 기준, 쉬운 해석을 한 흐름으로 제공한다.

## 2. 정보 구조와 사용자 흐름

### 홈

서비스의 범위를 설명하고 기업 검색을 최우선 작업으로 제공한다. 최근 확인 기업은 카드가 아닌 표로 보여줘 점수, PER, ROE와 핵심 판단을 바로 비교하게 한다.

### 기업분석

기업명 다음에 기업 체력 종합 판단을 제시하고, 핵심 지표 → AI 요약 → 이익 창출 흐름 → 재무 체력 → 장기 가치 평가 → 지속성 위험 순으로 상세 근거를 제공한다. 실시간 시세와 등락률은 제공하지 않는다.

### 종목찾기

사용자가 `80점 이상`, `저평가`, `실적 개선`, `현금흐름 우수`, `부채비율 낮음`, `ROE 높음` 조건을 선택해 기업을 줄이고 같은 열 기준으로 비교한다.

### 관심종목

분석 중 저장한 기업을 브라우저에 보관하고 한 표에서 다시 비교한다. 로그인이나 외부 데이터베이스가 없는 MVP이므로 같은 브라우저에서만 유지된다.

## 3. 핵심 기능

- 기업명·종목코드 검색과 빈 입력 안내
- 메뉴를 통한 4개 화면 이동
- 관심종목 추가·삭제와 `localStorage` 저장
- OpenDART 연결재무제표 조회 및 비율 계산
- 최근 5개 사업연도 매출·영업이익·순이익 차트
- Google Gemini를 이용한 초보자용 기업 분석 재작성
- 모바일 내비게이션과 데이터 표 가로 스크롤

## 4. AI 기능 설계

### 사용자 가치

사용자는 지표를 하나씩 해석하지 않아도 해당 기업의 수익성, 성장성, 안정성, 가격과 주의점을 짧은 한국어 설명으로 이해할 수 있다.

### 입력

- 검색으로 선택된 기업명
- 프론트엔드는 `{ "company": "삼성전자" }` 형식으로 서버에 전송
- 빈 문자열 금지, 최대 80자

### 처리

- Python Serverless Function이 `GEMINI_API_KEY`를 서버 환경 변수에서 읽음
- Gemini Interactions API에 분석 지침과 기업명을 전달
- 데이터가 부족한 경우 단정하지 않고 투자 권유를 하지 않도록 지시

### 출력

- 3~4문장의 쉬운 한국어 기업 분석
- 기존 분석 영역을 성공 응답으로 교체
- 사용 모델명은 API 응답에 포함하되 사용자 화면에는 불필요하게 강조하지 않음

### 실패 처리

- 빈 입력: HTTP 400과 `기업명을 입력해주세요.`
- 너무 긴 입력: HTTP 400과 80자 제한 안내
- 키 미설정: HTTP 503과 설정 필요 안내
- 외부 API 오류: HTTP 502 또는 500과 재시도 안내
- 요청 과다: HTTP 429와 잠시 후 재시도 안내
- 지연: 서버 20초 제한, 프론트 25초 제한과 지연 안내
- 실패해도 기존 기업 요약은 화면에 유지

## 5. 데이터 기준과 한계

- 실제 재무제표: 금융감독원 OpenDART 연결재무제표
- 지원 종목: 삼성전자, SK하이닉스, NAVER, 현대차, 기아
- 실시간 주가와 등락률: 제공하지 않음
- 관심종목: 서버 저장이 아닌 해당 브라우저의 `localStorage`
- AI 결과: 참고 설명이며 사실 확인과 투자 판단을 대신하지 않음

## 6. 기술 구조

- HTML: 문서 골격, 내비게이션, 앱 진입점
- CSS: 디자인 토큰, 정보 계층, 표와 반응형 레이아웃
- JavaScript: 화면 렌더링, 사용자 입력, 상태, `fetch`, 결과 반영
- Python Serverless Functions: 비밀 키 보호, OpenDART/Gemini 호출, 오류 응답
- Vercel: 정적 프론트와 Python 함수를 같은 도메인에서 배포

## 7. 성공 기준

- 사용자가 홈에서 기업을 검색해 분석 화면까지 이동할 수 있다.
- 최소 3개 이상의 화면을 메뉴로 이동할 수 있다.
- 실제 Gemini 응답이 분석 영역에 표시된다.
- API 실패 시 원인을 이해할 수 있는 메시지가 표시된다.
- 390px 모바일과 1440px 데스크톱에서 주요 기능이 잘리지 않는다.
- API 키가 저장소, 브라우저 코드, 문서와 스크린샷에 노출되지 않는다.
