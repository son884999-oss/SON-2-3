# QUEST.MD 준수 점검표

점검일: 2026-08-12

## 결론

코드와 로컬 문서로 구현할 수 있는 요구사항은 모두 반영했습니다. 제출 완료까지 남은 것은 계정 권한이 필요한 GitHub push, Vercel 배포·환경 변수 등록, 배포 주소 기입, 실제 배포 환경의 AI/OpenDART 화면 캡처입니다.

## 요구사항별 확인

| QUEST 요구사항 | 상태 | 구현 또는 확인 근거 |
|---|---|---|
| 웹 서비스 형태 | 완료 | `index.html`을 진입점으로 하는 반응형 SPA |
| 사용자가 값을 입력하고 결과 확인 | 완료 | 기업명·종목코드 검색 → 기업 분석 화면 |
| 3개 이상 화면과 메뉴 이동 | 완료 | 홈, 기업분석, 종목찾기, 용어사전, 관심종목 5개 화면 |
| 외부 API 또는 AI API 연동 | 완료 | OpenDART `GET /api/company`, Gemini `POST /api/analyze` |
| OpenDART 전체 기업 검색 | 완료 | `GET /api/search`, 기업명·종목코드 검색, `corpCode` 기반 조회 |
| 프론트엔드와 백엔드 구분 | 완료 | `index.html`·`css`·`js` / `api/*.py` 분리 |
| 서버에서 비밀 키 보호 | 완료 | Python 함수가 환경 변수로만 키를 읽음 |
| 입력 검증과 오류 처리 | 완료 | 빈 검색, 미지원 기업, 80자 제한, API 상태·시간 초과 안내 |
| AI 처리 중 상태 표시 | 완료 | 버튼 비활성화, `aria-busy`, 로딩 문구와 애니메이션 |
| 결과를 화면에 반영 | 완료 | OpenDART 수치·차트와 Gemini 문장을 기존 분석 영역에 반영 |
| 반응형 UI | 완료 | 820px·640px 분기, 모바일 하단 메뉴, 표 가로 스크롤 안내 |
| README·서비스 기획서 | 완료 | `README.md`, `SERVICE_PLAN.md` |
| 배포 URL·GitHub URL | 사용자 작업 | 각 계정 권한과 프로젝트 연결 필요 |
| 증빙 자료 | 일부 완료 | `evidence/` 로컬 캡처 존재. 실제 배포 AI 결과와 Codex 대화 캡처 필요 |

## 2차 UI/UX 재검토 반영

- 작동하지 않는 로그인 버튼을 제거했습니다.
- 기업 데이터는 반복 카드 대신 표와 인라인 지표로 비교합니다.
- 모바일 표에 좌우 스크롤 안내를 표시합니다.
- 용어사전 목차가 SPA 주소를 덮어쓰지 않도록 버튼식 스크롤로 변경했습니다.
- 관심종목은 `추가`/`해제` 상태를 명시하고 해제 후 `되돌리기`를 제공합니다.
- 상태 배너와 토스트에 화면 낭독기용 live region을 적용했습니다.
- 실시간 주가·등락률 대신 수익성·현금흐름·재무 안정성·가치 지속성을 우선합니다.
- 인라인 CSS를 제거하고 스타일을 `css/style.css`로 모았습니다.

## DESIGN.md anti-pattern 재점검

| 항목 | 결과 |
|---|---|
| 불필요한 카드·반복 카드 그리드 | 표, 목록, 구분선 중심 |
| 과도한 둥근 사각형·pill·badge | 버튼과 상태 표시에만 제한 |
| gradient·glassmorphism·shadow | 사용하지 않음 |
| 지나치게 큰 제목 | 반응형 `clamp()`와 42px 이하 위계 |
| 의미 없는 아이콘·장식 UI | 데모 로그인 제거, 장식 아이콘 없음 |
| 과도한 whitespace | 섹션 간격을 38~64px 범위로 제한 |
| 동일 중요도의 섹션 | 종합 판단 → 근거 → 추세 → 위험 순으로 위계 구분 |
| generic SaaS dashboard | 사이드바·KPI 카드 대신 검색 중심 정보 구조 |

## 제출 전에 사용자가 해야 할 일

1. GitHub에 commit/push합니다.
2. Vercel에서 저장소를 Import하고 환경 변수를 등록합니다.
3. 배포 URL에서 OpenDART와 Gemini 응답을 확인합니다.
4. README의 GitHub·Vercel 주소를 실제 주소로 교체합니다.
5. 배포된 데스크톱, 모바일, 실제 AI 결과, 이번 Codex 대화를 캡처합니다.
